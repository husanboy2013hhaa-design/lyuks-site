.vercel
.env
.env*.local

node_modules

# The bot lives in bot/ and deploys to Render (Root Directory: bot).
# Its secrets and runtime files must never be committed — the token
# belongs in Render's environment variables, not in the repo.
bot/.env
bot/.env.*
bot/__pycache__/
bot/state.json
*.pyc
